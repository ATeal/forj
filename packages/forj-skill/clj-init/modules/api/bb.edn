{:paths ["src"]

 :tasks
 {dev {:doc "Start backend with nREPL"
       :task (shell "clj" "-M:dev")}

  repl {:doc "Start nREPL server"
        :override-builtin true
        :task (shell "clj" "-M:dev")}

  test {:doc "Run tests"
        :task (shell "clj" "-M:test")}

  run {:doc "Run server"
       :override-builtin true
       :task (shell "clj" "-M" "-m" "{{project-name}}.core")}}}
