{:paths ["src"]

 :tasks
 {dev {:doc "Start backend with nREPL"
       :task (shell "clojure" "-M:dev")}

  repl {:doc "Start nREPL server"
        :override-builtin true
        :task (shell "clojure" "-M:dev")}

  test {:doc "Run tests"
        :task (shell "clojure" "-M:test")}

  run {:doc "Run server"
       :override-builtin true
       :task (shell "clojure" "-M" "-m" "{{project-name}}.core")}}}
