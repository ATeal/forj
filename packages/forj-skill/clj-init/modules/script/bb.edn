{:paths ["src"]
 :tasks
 {run {:doc "Run the script"
       :task (shell "bb" "-m" "{{project-name}}.core")}

  nrepl {:doc "Start nREPL server"
         :override-builtin true
         :task (shell "bb" "nrepl-server")}}}
